# Database Configuration
# For local development with Docker Compose, use the postgres service hostname
# For production, use your external database connection details
DB_HOST=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=website-dummy
DB_PORT=5432
DB_SSLMODE=disable
# For production, set DB_SSLMODE=require

# Backend Configuration
BACKEND_PORT=8080
GIN_MODE=debug
GO_ENV=development

# Properties API Configuration
# Use host.docker.internal for local development (works on Mac/Windows)
# Use anhelm-api:8080 if containers are on the same network
PROPERTIES_API_URL=http://host.docker.internal:8003

# Frontend Configuration
NODE_ENV=development
# SITE_URL is used for frontend API calls to the backend
# For local development, this should be the backend URL (port 8080)
SITE_URL=http://localhost:8080

# Auth0 Configuration
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_AUDIENCE=your-auth0-api-identifier

# Email Configuration (Local Development with MailHog)
# - If backend runs in Docker: use SMTP_HOST=mailhog
# - If backend runs locally: use SMTP_HOST=localhost
# 
# For local development, MailHog is used (no SendGrid API key needed)
# For production, set SENDGRID_API_KEY instead (see env.production.example)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@website-dummy.com
NOTIFICATION_EMAILS=admin@website-dummy.com

# SendGrid API Key (only needed for production)
# SENDGRID_API_KEY=  # Leave empty for local development

# Bunny CDN Configuration
# Get these from your Bunny CDN dashboard:
# - Storage Zone: The NAME of your storage zone (NOT the FTP username)
#   Go to Storage > Your Storage Zone > The name shown at the top (e.g., "website-dummy-com")
# - Access Key: Your storage zone PASSWORD from FTP & API Access tab (NOT the FTP username)
#   Go to Storage > Your Storage Zone > FTP & API Access > Copy the "Storage Zone Password"
# - Storage Hostname: The region-specific storage hostname (e.g., ny.storage.bunnycdn.com)
#   Go to Storage > Your Storage Zone > FTP & API Access > Copy the "Hostname" (e.g., "ny.storage.bunnycdn.com")
#   If not set, defaults to "storage.bunnycdn.com"
# - CDN Hostname: Your pull zone hostname (e.g., website-dummy.b-cdn.net)
#   Go to Pull Zones > Your Pull Zone > The hostname (e.g., "website-dummy-com.b-cdn.net")
BUNNY_STORAGE_ZONE=your-storage-zone-name
BUNNY_STORAGE_ACCESS_KEY=your-storage-zone-password
BUNNY_STORAGE_HOSTNAME=ny.storage.bunnycdn.com
BUNNY_CDN_HOSTNAME=your-cdn-hostname.b-cdn.net

