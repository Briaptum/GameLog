# Production Environment Variables
# Copy this file to .env on your production server and fill in the values

# Database Configuration (Production)
# Use your production database connection details
DB_HOST=your-production-db-host.com
DB_USER=your-db-user
DB_PASSWORD=your-secure-db-password
DB_NAME=website-dummy
DB_PORT=5432
DB_SSLMODE=require

# Backend Configuration
BACKEND_PORT=8004
GIN_MODE=release
GO_ENV=production

# Properties API Configuration
# Point to your production properties API
# Using shared Docker network, use the API container name and internal port: http://anhelm-api-prod:8080
# If API is on different server, use the full URL: http://api.yourdomain.com
PROPERTIES_API_URL=http://anhelm-api-prod:8080

# Frontend Configuration
NODE_ENV=production
# SITE_URL is used for both frontend API calls and CORS configuration
# Point to your production frontend URL (the backend will allow this origin for CORS)
SITE_URL=https://website-dummy.anhelm.com

# Auth0 Configuration (Production)
# Use your production Auth0 credentials
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_CLIENT_ID=your-production-client-id
AUTH0_CLIENT_SECRET=your-production-client-secret
AUTH0_AUDIENCE=your-production-api-identifier

# Email Configuration (Production)
# 
# RECOMMENDED: Use SendGrid Web API (more reliable than SMTP)
# SENDGRID_API_KEY=your-sendgrid-api-key-here  # Get this from SendGrid Dashboard > Settings > API Keys
# SMTP_FROM=noreply@website-dummy.com  # MUST be a verified sender in SendGrid
# NOTIFICATION_EMAILS=admin@website-dummy.com  # Comma-separated list of emails to notify
#
# ALTERNATIVE: Use SendGrid SMTP (less reliable, not recommended)
# If SENDGRID_API_KEY is not set, the service will fall back to SMTP:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey  # This is literal "apikey", not your actual API key
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=noreply@website-dummy.com
# NOTIFICATION_EMAILS=admin@website-dummy.com
#
# IMPORTANT: SendGrid REQUIRES sender verification. Emails will NOT be sent if the sender is not verified.
# You can verify your sender in two ways:
# 1. Domain Authentication (recommended for production): Verify your entire domain in SendGrid dashboard
#    - This allows sending from any email @yourdomain.com
#    - Requires adding DNS records to your domain
# 2. Single Sender Verification: Verify just one email address
#    - Quick setup but less secure
#    - Go to SendGrid Dashboard > Settings > Sender Authentication

# SendGrid Web API (RECOMMENDED)
SENDGRID_API_KEY=your-sendgrid-api-key-here
SMTP_FROM=noreply@website-dummy.com
NOTIFICATION_EMAILS=admin@website-dummy.com

# Bunny CDN Configuration
# Get these from your Bunny CDN dashboard:
# - Storage Zone: The NAME of your storage zone (NOT the FTP username)
#   Go to Storage > Your Storage Zone > The name shown at the top (e.g., "website-dummy-com")
# - Access Key: Your storage zone PASSWORD from FTP & API Access tab (NOT the FTP username)
#   Go to Storage > Your Storage Zone > FTP & API Access > Copy the "Storage Zone Password"
# - Storage Hostname: The region-specific storage hostname (e.g., ny.storage.bunnycdn.com)
#   Go to Storage > Your Storage Zone > FTP & API Access > Copy the "Hostname" (e.g., "ny.storage.bunnycdn.com")
#   If not set, defaults to "storage.bunnycdn.com"
# - CDN Hostname: Your pull zone hostname (e.g., website-dummy.b-cdn.net)
#   Go to Pull Zones > Your Pull Zone > The hostname (e.g., "website-dummy-com.b-cdn.net")
BUNNY_STORAGE_ZONE=your-storage-zone-name
BUNNY_STORAGE_ACCESS_KEY=your-storage-zone-password
BUNNY_STORAGE_HOSTNAME=ny.storage.bunnycdn.com
BUNNY_CDN_HOSTNAME=your-cdn-hostname.b-cdn.net

